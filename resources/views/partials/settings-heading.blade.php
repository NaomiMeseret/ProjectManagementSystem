<div class="relative mb-8 w-full space-y-3">
    <p class="pm-eyebrow">{{ __('Account Workspace') }}</p>
    <h1 class="pm-title text-2xl md:text-3xl">{{ __('Settings') }}</h1>
    <p class="pm-subtitle">{{ __('Manage your profile, credentials, and security preferences.') }}</p>
    <div class="pm-glow-divider"></div>
</div>
